import 'package:flutter/material.dart';

class UserPosts {
  UserPosts(
      {@required this.title,
      @required this.attachment,
      @required this.description});
  String title;
  String attachment;
  String description;
  // Tim timepost;
}
